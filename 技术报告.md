# 基于深度学习的牛津宠物细粒度分类实验报告

姓名：韩承翰 ｜ 学号：W124301159 ｜ GitHub：https://github.com/hch1256/oxford-pet-fine-grained

---

## 1. 任务背景与实验设置

**数据集**：Oxford-IIIT Pet，37 类（25 种犬 + 12 种猫）、共 7,349 张图像。该数据集属于**细粒度分类**——类别间差异往往只在耳型、毛色分布、面部斑纹等局部区域，而同种动物的姿态、光照、背景差异极大，因此难度显著高于普通 37 分类任务。

**数据划分**：合并官方 `trainval`(3,680) 与 `test`(3,669) 两个 split 得到全量数据，再用 `sklearn` 做**两步分层抽样**，按 70:15:15 划分为 5,144 / 1,102 / 1,103 张，`random_state=42` 保证可复现。分层抽样确保每个品种在三份数据中的比例严格一致。该数据集类别分布较为均衡（每类 184~200 张），但验证集仅 1,102 张、平均每类约 30 张，若完全随机划分，个别品种的验证样本数仍会明显偏离均值，使 Macro-F1 这类按类等权平均的指标产生波动。分层抽样消除了这一不确定性，也让三组消融实验之间的横向对比更可靠。

**防止数据泄漏**：训练集 Transform 含 `RandomResizedCrop` / `RandomHorizontalFlip` / `Normalize`，验证集与测试集**仅含** `Resize + CenterCrop + Normalize`，不含任何随机成分。测试集全程未参与训练与模型选择（模型选择只依据验证集）。

**硬件与超参数**：

| 项目 | 配置 |
| :--- | :--- |
| 硬件 | NVIDIA RTX 4060 Laptop (8GB)，PyTorch 2.14.0 + CUDA 12.6 |
| 骨干网络 | ResNet-18（ImageNet 预训练权重），替换 `fc` 层为 `Linear(512, 37)` |
| 优化器 | AdamW，lr = 1e-4，weight_decay = 1e-4，**无学习率调度器（全程恒定）** |
| 批大小 / 轮数 | 32 / 15 epochs |
| 精度 | 混合精度训练（`torch.amp.autocast` + `GradScaler`），约提速 20~40% |

**关于损失函数**：模型输出**裸 logits，不加 Softmax**——`nn.CrossEntropyLoss` 内部已包含 `log_softmax`，额外加 Softmax 会导致梯度消失、训练停滞。

---

## 2. 实验结果与消融分析

所有指标均在**测试集（1,103 张）**上测得。消融实验遵循**单变量控制原则**：每组仅修改一个参数，其余全部保持默认。

| 实验配置 | Top-1 Acc (%) | Top-5 Acc (%) | Macro-F1 | 训练轮数/耗时 |
| :--- | :--- | :--- | :--- | :--- |
| 1. Baseline（ResNet-18 基础增强） | 90.84 | **99.27** | 0.9080 | 15 ep / 15.5 min |
| 2. 改进组 A（+ RandAugment） | 90.93 | 99.18 | 0.9084 | 15 ep / 17.1 min |
| 3. 改进组 B（+ Label Smoothing 0.1） | **92.38** | 98.91 | **0.9230** | 15 ep / 17.8 min |

> 说明：任务书模板中的改进组 A 示例为 MixUp。本项目选择 **RandAugment**，因其在细粒度任务中更常用——它通过随机组合 2 个增强算子、统一强度参数控制扰动幅度，比 MixUp 的样本线性插值更贴合「局部特征判别」的需求。
>
> 耗时在 15.5~17.8 min 区间波动（单轮 46~94 s），原因是笔记本 GPU 在训练期间存在其他负载与温度降频，非算法差异。

### 曲线分析

![收敛曲线](report_figures/fig1_curves.png)

**（a）Baseline 存在明显的过拟合。** 训练损失单调降至 0.038，而验证损失在第 8 轮触底（0.261）后**持续反弹至 0.319，涨幅 +0.058**；终轮 train/val 损失差达 0.281。模型在后 7 轮已从「学习泛化特征」转为「记忆训练样本」。

**（b）Label Smoothing 显著抑制了过拟合。** 其损失绝对值不可与 (a) 横向比较——Label Smoothing 把硬标签软化为「0.9 正确类 + 0.1 均匀分布」，理论损失下界约为 `0.1 × ln(37) ≈ 0.36`，量纲已改变（图中红线标注）。真正可比的是**过拟合的幅度**：其验证损失在第 10 轮触底（0.925）后仅反弹至 0.941，**涨幅 +0.016，仅为 Baseline 的 1/4**；终轮 train/val 差距收窄至 0.203。这说明软化标签削弱了模型对训练集极端置信的追求。

**（c）准确率对比。** Label Smoothing 曲线整体上移，第 10 轮达到峰值 93.56%（验证集）。RandAugment 曲线与 Baseline 几乎重合，峰值仅高出 0.18pp。

### 消融结论

1. **RandAugment 在本任务上收益有限**：测试集 Top-1 仅提升 0.09pp（约 1 个样本），不具备统计显著性。但其正则化作用真实存在——训练损失终值由 0.038 升至 0.097（**2.5 倍**），train/val 泛化差距由 0.281 缩小至 0.221（**−21.5%**）。这说明模型记忆训练集的能力被有效抑制，只是这部分收益未能转化为测试集精度。

2. **Label Smoothing 效果显著**：测试集 Top-1 提升 **1.54pp**，Macro-F1 提升 **0.015**，且是在参数量、训练轮数、数据完全相同的前提下取得的。

3. **Top-5 与 Top-1 的权衡**：Label Smoothing 的 Top-5 反而略降（99.27% → 98.91%）。原因是软化标签压制了模型对正确答案的极端自信，部分本可排入前 5 的正确答案被「平均化」挤出——属于该方法的固有代价，而非缺陷。在只关心 Top-1 的场景下这一交换是划算的。

---

## 3. 错误案例分析（Bad Case）与 Grad-CAM 可视化

### 3.1 混淆矩阵局部图

下图从 37×37 的混淆矩阵中裁出**最容易混淆的 8 个品种**（覆盖 Baseline 中错分最严重的 4 个类别对），左右对比 Baseline 与 Label Smoothing。斜线为正确分类，非斜线格即为错分。

![混淆矩阵局部对比](report_figures/fig2_cm_zoom.png)

各类别对的错分次数变化（格式 `A→B | B→A`）：

| 类别对 | Baseline | + RandAugment | + Label Smoothing |
| :--- | :--- | :--- | :--- |
| American Pit Bull Terrier ↔ Staffordshire Bull Terrier | 8 \| 1（合计 9） | 5 \| 2（合计 7） | **2 \| 1（合计 3）** |
| Bengal ↔ Egyptian Mau | 4 \| 4（合计 8） | 3 \| 2（合计 5） | **4 \| 6（合计 10）** |
| Chihuahua ↔ Miniature Pinscher | 7 \| 0（合计 7） | 6 \| 1（合计 7） | 7 \| 0（合计 7） |
| Birman ↔ Ragdoll | 1 \| 4（合计 5） | 2 \| 1（合计 3） | 2 \| 1（合计 3） |

**三条关键发现：**

1. **最严重的错误集中在生物学近缘品种上。** 美国比特犬与斯塔福郡斗牛㹴同属斗牛㹴类，体型、头骨结构高度相似；孟加拉猫与埃及猫同属短毛斑点猫；缅甸猫与布偶猫都是蓝眼重点色长毛猫。模型犯错的不是「随机图片」，而是**人类专家也需要专业训练才能区分的类别**——这符合细粒度分类的典型失败模式。

2. **Label Smoothing 的收益具有选择性。** 它对比特犬↔斯塔福改善显著（9 → 3，**降幅 67%**），但对**吉娃娃→迷你杜宾完全无效**（7 → 7，纹丝不动），甚至使**孟加拉猫↔埃及猫恶化**（8 → 10）。合理推测：该方法的软化标签放宽了决策边界，对「边界模糊」的类别对有利；而吉娃娃与迷你杜宾的视觉特征重合度过于直接（同为小型短毛犬），仅靠正则化无法解决，需要更强的局部特征建模能力（如注意力机制或更高输入分辨率）。**这条负面结果是本实验最有价值的信息之一——它划出了 Label Smoothing 的能力边界。**

3. **吉娃娃→迷你杜宾的错误是完全单向的。** Baseline 与 Label Smoothing 均为 `7 | 0`，即**没有任何一个迷你杜宾被误判为吉娃娃**，反方向 0 错误；但在混淆矩阵中迷你杜宾一行 30 个样本全部正确（30/30）。这不是对称的「混淆」，而是**系统性的单向偏置**：模型学到的判别边界把一部分吉娃娃划进了迷你杜宾的区域。仅提高正则化强度无法修正这类偏置，需要类别重加权或针对性的难例挖掘。

### 3.2 Grad-CAM 热力图

Grad-CAM 通过计算目标类别分数对 `layer4[-1]` 特征图的梯度，加权求和得到「模型做决策时真正关注的区域」。以下两张图取自最终模型（+ Label Smoothing）。

**（1）预测正确样例**

![预测正确样例](runs/ls01/gradcam_correct.png)

真实类别 English Setter，预测 English Setter，置信度 **99.9%**。热力图高亮区域集中在**犬只的面部**（眼睛、口鼻、耳根），背景草地与身体毛发几乎无响应。说明模型**没有**依赖「草地上、白色长毛」这类上下文捷径，而是聚焦于区分品种所需的头部形态特征——这是模型学到正确特征的直接证据。

**（2）预测错误样例**

![预测错误样例](runs/ls01/gradcam_error.png)

真实类别 Birman（缅甸猫），预测 Ragdoll（布偶猫），置信度 **94.6%**。热力图同样密集覆盖**猫的面部**（双眼、鼻梁、口鼻区域）。

这个案例的价值在于：**模型关注的区域本身是正确的**。缅甸猫与布偶猫的区分依据恰恰是面部与肢端的毛色分布、眼睛颜色深浅、面部斑纹形状——模型正是在这些区域下了判断，只是两者的视觉差异过于细微（同为蓝眼重点色长毛猫），模型未能捕捉到足以区分的特征强度。**因此这不是「注意力跑偏」型错误，而是「特征判别力不足」型错误**，对应的改进方向是提高输入分辨率或在 `layer4` 后引入注意力模块，而非修正注意力区域。

---

## 4. AI 辅助编程声明与 Bug 复盘

**使用环节**：本项目在开发中使用 AI 编程工具辅助，主要用于——生成 Grad-CAM 可视化代码与训练循环的初稿、编写 TensorBoard 记录逻辑、以及代码模块化结构建议。**所有 AI 生成代码均经过本人实际运行验证**，下列 Bug 均为运行报错后定位修复。

**Bug 复盘 1：数据加载导致内存爆炸（最严重）**

AI 最初生成的数据集代码用 `list(train_dataset) + list(test_dataset)` 合并两个 split，再整体划分。这个写法在逻辑上等价于把**全部 7,349 张图像解码进内存**（约 4~5 GB）。配合 `num_workers=4` 时问题急剧放大——DataLoader 会把数据集对象 **pickle 复制给每一个子进程**，4 个 worker 就是 4 份完整图像副本，总内存需求超过 20 GB，进程被操作系统直接杀死。

**排查过程**：报错本身只是「进程意外终止」，没有明确指向内存。通过查阅 torchvision 安装目录下 `OxfordIIITPet` 的源码，确认其内部只保存 `self._images`（`pathlib.Path` 列表）与 `self._labels`，真正的图像读取发生在 `__getitem__` 中——即官方实现本身就是**延迟加载**的，是我的合并方式破坏了这个特性。

**修复**：重写 `PetDataset`，只存储 `(图片路径, 标签)` 元组，在 `__getitem__` 中才执行 `Image.open(path).convert("RGB")`。内存占用降至可忽略，`num_workers=4` 正常工作。这条经验具有普遍性：**只要 DataLoader 使用多进程，数据集对象就必须只存轻量引用，绝不能存解码后的图像。**

**Bug 复盘 2：混淆矩阵绘制崩溃**

绘制 37 类混淆矩阵时抛出 `ValueError: Unknown format code 'd' for object of type 'float'`。定位发现：代码在绘制前统一执行了 `cm.astype(np.float64)`，而 `seaborn.heatmap` 又用 `fmt="d"`（整数格式）渲染这个浮点数组，格式码与数据类型不匹配。修复方式是仅在**行归一化分支**转浮点并使用 `fmt=".2f"`，计数分支保持整数类型与 `fmt="d"`。

**Bug 复盘 3：图中中文渲染为方框**

matplotlib 默认字体 DejaVu Sans 不含中文字形，图中所有中文标题显示为空白方框（终端同时报 `Glyph missing from font(s)` 警告）。修复方式是在绘图脚本开头设置 `plt.rcParams["font.sans-serif"] = ["Microsoft YaHei", "SimHei", "DejaVu Sans"]` 并关闭 `axes.unicode_minus`，使中文与负号正常显示。

---

## 附：复现方式

```bash
# 环境
conda create -n pet python=3.11 -y && conda activate pet
pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu126
pip install -r requirements.txt

# 三组实验
python train.py --amp --out_dir ./runs/baseline
python train.py --amp --randaugment --out_dir ./runs/randaug
python train.py --amp --label_smoothing 0.1 --out_dir ./runs/ls01

# 评估与可视化
python evaluate.py --ckpt ./runs/ls01/best_model.pth

# 生成报告图表
python make_report_figures.py

# 训练曲线
python -m tensorboard.main --logdir ./runs --port 6006
```

全部实验总耗时约 50 分钟，完整输出（权重、混淆矩阵、Grad-CAM、误判清单、TensorBoard 日志）见 GitHub 仓库。
