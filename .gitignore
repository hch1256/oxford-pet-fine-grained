# 数据集（约 800MB，不纳入版本控制）
data/oxford-iiit-pet/

# 模型权重（每个约 45MB）
*.pth

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# 编辑器
.vscode/
.idea/

# 交付用的打包文件（内容已全部在仓库内，不重复提交）
*.zip

# 技术报告：作为邮件附件单独提交，不纳入版本控制
技术报告.md
*.pdf

# 注意：runs/ 下的 TensorBoard 日志(events.out.tfevents*)与
# history.json 体积很小，需要提交作为「训练曲线日志」交付物，因此不忽略。
